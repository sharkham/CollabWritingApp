<h1>Edit your bio:</h1>

<%= form_for(@user) do |f| %>
  <%= f.label :bio %><br>
  <%= f.text_area :bio %>
  <br>
  <%= f.submit "Submit" %>

<% end %>
<br>
<%= link_to "Back to your profile", user_path(current_user) %>
