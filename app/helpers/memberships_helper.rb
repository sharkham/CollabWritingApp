module MembershipsHelper

  # def sort_alphabetically(novel)
  #   memberships = novel.memberships
  #   #order memberships alphabetically by username
  #   #membership has user_id field
  #   #get the user from the id and sort that alphabetically
  #   new_array = memberships.map do |membership|
  #     membership.user
  #   end
  #   new_array.order('username')

  #   # memberships.joins().order_by()
  #   # memberships.where()
  #   #find each user by user_id that memberships has available to it
  #   #chain from memberships to find users through memberships user_id attribute
  # end
end
